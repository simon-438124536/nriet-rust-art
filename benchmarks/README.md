# Native slice microbenchmarks

Run locally only; outputs are not committed.

```powershell
cd F:\nriet-rust-art\repo
python benchmarks\run_slice_benchmark.py --slice smooth_and_trim_scan
```

Results are written under `.tmp/benchmarks/` by default.
